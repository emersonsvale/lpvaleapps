# Exemplo de variáveis de ambiente para Vale Apps
# Copie este arquivo para .env e configure conforme necessário

# Ambiente de execução
NODE_ENV=production

# Configurações do servidor Nitro
NITRO_PORT=3000
NITRO_HOST=0.0.0.0
NITRO_PRESET=node-server

# Configurações de performance
NODE_OPTIONS=--max-old-space-size=1024
UV_THREADPOOL_SIZE=4

# URLs base (configure conforme seu domínio)
NUXT_PUBLIC_SITE_URL=https://valeapps.com.br
NUXT_PUBLIC_API_BASE=/api

# Google Analytics (opcional - já configurado no nuxt.config.ts)
# NUXT_PUBLIC_GTAG_ID=G-JMXFDMLNQE

# Meta Pixel (opcional - já configurado no nuxt.config.ts)
# NUXT_PUBLIC_FACEBOOK_PIXEL_ID=1143371970995141

# Google Tag Manager (opcional - já configurado no nuxt.config.ts)
# NUXT_PUBLIC_GTM_ID=GTM-PRS7FS5H

# Configurações de segurança (opcional)
# NUXT_SECRET_KEY=your-secret-key-here

# Configurações de email (se implementar formulário de contato)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=contato@valeapps.com.br
# SMTP_PASS=your-app-password

# Supabase (obrigatório para seção de projetos e dados dinâmicos)
NUXT_PUBLIC_SUPABASE_URL=https://seu-projeto.supabase.co
NUXT_PUBLIC_SUPABASE_ANON_KEY=sua-chave-anon-aqui

# Configurações de banco de dados (se necessário no futuro)
# DATABASE_URL=postgresql://user:password@localhost:5432/valeapps

# Configurações de cache (Redis, se necessário)
# REDIS_URL=redis://localhost:6379

# Configurações de CDN (se usar)
# CDN_URL=https://cdn.valeapps.com.br

# Configurações de monitoramento (opcional)
# SENTRY_DSN=your-sentry-dsn
# MONITORING_ENDPOINT=https://monitoring.valeapps.com.br